/*
*Generated by GeniusPlatform. 2016-05-06 10:03:56
*/
package com.genius.BOSS.medical_r.action;

import java.io.IOException;

import javax.servlet.ServletException;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;

import org.jdom.Document;
import org.jdom.Element;
import org.jdom.output.Format;
import org.jdom.output.XMLOutputter;
import org.springframework.context.ApplicationContext;
import org.springframework.context.support.ClassPathXmlApplicationContext;
import org.springframework.web.servlet.ModelAndView;
import org.springframework.web.servlet.mvc.Controller;

import org.genius.util.*;
import com.genius.BOSS.medical_r.IMedical_r;

public final class InsertEditAction implements Controller {
	public ModelAndView handleRequest(HttpServletRequest request,
			HttpServletResponse response) throws ServletException, IOException {

		HttpSession session = request.getSession();
		
		if (session.getAttribute("login") == null
				|| (!session.getAttribute("login").equals("true"))) {
			Element root = new Element("entity");
			
			Element actionState = new Element("actionState");
			Element fieldState = new Element("field");
			fieldState.setAttribute("name", "state");
			fieldState.setAttribute("value", "noSession");
			actionState.addContent(fieldState);

			root.addContent(actionState);
			Document doc = new Document(root);
			response.setContentType("application/xml;charset=GB2312");
			Format format = Format.getPrettyFormat();
			format.setEncoding("GB2312");
			XMLOutputter outer = new XMLOutputter(format);
			outer.output(doc, response.getWriter());

			return null;
		}
		
		String success = "true";

		String ID = java.net.URLDecoder.decode(request.getParameter("ID"),"UTF-8");
		String NAME = java.net.URLDecoder.decode(request.getParameter("NAME"),"UTF-8");
		String SEX = java.net.URLDecoder.decode(request.getParameter("SEX"),"UTF-8");
		String AGE = java.net.URLDecoder.decode(request.getParameter("AGE"),"UTF-8");
		String DISEASE = java.net.URLDecoder.decode(request.getParameter("DISEASE"),"UTF-8");
		String DISCRIBE = java.net.URLDecoder.decode(request.getParameter("DISCRIBE"),"UTF-8");
DISCRIBE=TextFormat.getSqlText(DISCRIBE);
		String case_imgs = java.net.URLDecoder.decode(request.getParameter("case_imgs"),"UTF-8");
		String SECTION = java.net.URLDecoder.decode(request.getParameter("SECTION"),"UTF-8");
		String CREATE_USER_ID = java.net.URLDecoder.decode(request.getParameter("CREATE_USER_ID"),"UTF-8");
		String CREATE_TIME = java.net.URLDecoder.decode(request.getParameter("CREATE_TIME"),"UTF-8");
		String STATE = java.net.URLDecoder.decode(request.getParameter("STATE"),"UTF-8");
		String RECIEVE_USER_ID = java.net.URLDecoder.decode(request.getParameter("RECIEVE_USER_ID"),"UTF-8");
		String SOLVE_TIME = java.net.URLDecoder.decode(request.getParameter("SOLVE_TIME"),"UTF-8");
		String SOLVE_CONTENT = java.net.URLDecoder.decode(request.getParameter("SOLVE_CONTENT"),"UTF-8");
		String is_jump_ask = java.net.URLDecoder.decode(request.getParameter("is_jump_ask"),"UTF-8");


		ApplicationContext context = new ClassPathXmlApplicationContext(
				"applicationContext.xml");

		IMedical_r medical_r = (IMedical_r) context
				.getBean("medical_rProxy");
		medical_r.setSId(session.getAttribute("userId").toString());
		try {
			if (medical_r.insertEdit(DISCRIBE,DISEASE,case_imgs,NAME,RECIEVE_USER_ID,SECTION,SOLVE_CONTENT,AGE,STATE,SOLVE_TIME,ID,CREATE_USER_ID,is_jump_ask,SEX,CREATE_TIME)) {
				success = "true";
			} else {
				success = "false";
			}
		} catch (NullPointerException e) {
			success = "noPower";
		}

		Element root = new Element("entity");
		Element row = new Element("row");
		Element field1 = new Element("field");
		field1.setAttribute("name", "success");
		field1.setAttribute("value", success);
		row.addContent(field1);

		root.addContent(row);
		
		Element actionState = new Element("actionState");
		Element fieldState = new Element("field");
		fieldState.setAttribute("name", "state");
		fieldState.setAttribute("value", "session");
		actionState.addContent(fieldState);
		root.addContent(actionState);

		Document doc = new Document(root);
		response.setContentType("application/xml;charset=GB2312");
		Format format = Format.getPrettyFormat();
		format.setEncoding("GB2312");
		XMLOutputter outer = new XMLOutputter(format);
		outer.output(doc, response.getWriter());

		return null;
	}
}

